local Scanner = {}

function Scanner:Scan(callback)
    for _, obj in ipairs(game:GetDescendants()) do
        if obj:IsA("LocalScript") and obj.Source:find("Kick") then
            callback(obj:GetFullName())
        end
    end
end

return Scanner
